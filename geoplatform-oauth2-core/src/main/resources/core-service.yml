server:
  type: default
  maxThreads: 1024
  minThreads: 8
  maxQueuedRequests: 1024

server:
  applicationConnectors:
    - type: http
      port: 8082
      headerCacheSize: 512 bytes
      outputBufferSize: 32KiB
      maxRequestHeaderSize: 8KiB
      maxResponseHeaderSize: 8KiB
      inputBufferSize: 8KiB
      idleTimeout: 30 seconds
      minBufferPoolSize: 64 bytes
      bufferPoolIncrement: 1KiB
      maxBufferPoolSize: 64KiB
      acceptorThreads: 1
      selectorThreads: 2
      acceptQueueSize: 1024
      reuseAddress: true
      soLingerTime: 345s
      useServerHeader: false
      useDateHeader: true
      useForwardedHeaders: true

server:
 applicationConnectors: 
 -  type: http
    port: 8082    
 adminConnectors:
 -  type: http
    port: 8081

coreAuth:
  authorizationServerUrl: http://localhost:8080/v1/tokeninfo
  secret: af1cf471-0772-4f51-ba30-6900b0b56840
  key: faad82dd-50d7-4380-872f-af1a1584e2ae

# Logging settings.
logging:
  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: info
  # Logger-specific levels.
  loggers:
      "org.geosdi.geoplatform.dropwizard" : debug
      "org.geosdi.geoplatform.responce.factory" : debug
      appenders:
        type: console